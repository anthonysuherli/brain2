{
  "description": "SessionStart guard: on a new repo, seeds a brain2 KB and offers the KG schema wizard; on an existing KB, asks the drift detector whether to surface a (debounced, non-blocking) schema-(re)design offer.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "python ${CLAUDE_PLUGIN_ROOT}/hooks/first-run-init.py",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
