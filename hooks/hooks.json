{
  "description": "SessionStart first-run detection: seeds a brain2 KB and offers the KG schema wizard on first open of a new repo.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "python ${CLAUDE_PLUGIN_ROOT}/hooks/first-run-init.py",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
